# GeoCustody Environment Configuration
# Copy this file to .env and customize values

# Application
APP_NAME=GeoCustody
DEBUG=true

# Database (SQLite for demo)
DATABASE_URL=sqlite:///./data/geocustody.db

# JWT Authentication
SECRET_KEY=change-this-to-a-secure-secret-key-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS Origins (comma-separated)
CORS_ORIGINS=["http://localhost:5173","http://localhost:5174"]

# ===============================================
# Telefónica Open Gateway Configuration
# ===============================================
# Mode options:
#   - mock: Local mock mode (default) - Uses frontend mock panel for testing
#   - sandbox: Telefónica Sandbox - Uses CIBA OAuth flow with real sandbox APIs
#   - production: Telefónica Production - Uses CIBA OAuth flow with production APIs
GATEWAY_MODE=mock

# OAuth Credentials (required for sandbox and production mode)
# Get these from https://developers.opengateway.telefonica.com/ (Developer Hub)
# GATEWAY_CLIENT_ID=your-client-id
# GATEWAY_CLIENT_SECRET=your-client-secret

# Base URL Override (optional - auto-selected based on mode)
# Sandbox: https://sandbox.opengateway.telefonica.com/apigateway
# Production: https://opengateway.telefonica.com/apigateway
# GATEWAY_BASE_URL=

# ===============================================
# Telefónica Open Gateway API Reference
# ===============================================
# Authorization Flow (CIBA - Client-Initiated Backchannel Authentication):
#   1. POST /bc-authorize - Get auth_req_id with login_hint (phone) and scope
#   2. POST /token - Exchange auth_req_id for access token
#   3. Use Bearer token for API calls
#
# Number Verification: POST /number-verification/v0/verify
#   Scope: dpv:FraudPreventionAndDetection#number-verification-verify-read
#
# Location Verification: POST /device-location/v0/verify
#   Scope: dpv:FraudPreventionAndDetection#device-location-read
#
# SIM Swap Check: POST /sim-swap/v0/check
#   Scope: dpv:FraudPreventionAndDetection#sim-swap
#
# SIM Swap Retrieve: POST /sim-swap/v0/retrieve-date
#   Scope: dpv:FraudPreventionAndDetection#sim-swap
#
# Device Swap Check: POST /device-swap/v0.1/check
#   Scope: dpv:FraudPreventionAndDetection#device-swap
#
# Device Swap Retrieve: POST /device-swap/v0.1/retrieve-date
#   Scope: dpv:FraudPreventionAndDetection#device-swap
#
# Documentation: https://developers.opengateway.telefonica.com/reference
